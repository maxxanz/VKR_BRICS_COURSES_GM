<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BRICSCourses">  <!-- ЭТА СТРОКА УЖЕ ЕСТЬ, НЕ МЕНЯЙТЕ -->

        <!-- AuthActivity -->
        <activity
            android:name=".AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- LessonsActivity -->
        <activity
            android:name=".ui.lessons.LessonsActivity"
            android:exported="false" />

        <!-- LessonDetailActivity -->
        <activity
            android:name=".ui.lessons.LessonDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.test.TestActivity"
            android:exported="false" />

        <activity
            android:name=".ui.test.TestResultActivity"
            android:exported="false" />

        <activity
            android:name=".ui.coursedetail.CourseDetailActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

        <activity android:name=".ui.teacher.AddQuestionActivity" />

    </application>

</manifest>